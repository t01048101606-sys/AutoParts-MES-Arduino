# WSL2 환경에서 아두이노 USB 연동 및 Streamlit 실시간 모니터링 가이드

본 가이드는 Windows에 연결된 아두이노(Arduino) USB 장치를 WSL2(Windows Subsystem for Linux) 환경에 공유하고, `pyserial`을 사용하여 Streamlit 웹 애플리케이션과 실시간 데이터 통신을 수행하는 전체 가이드입니다.

---

## 📋 목차
1. [사전 준비](#1-사전-준비)
2. [Windows 환경: usbipd 설정 및 WSL 연결](#2-windows-환경-usbipd-설정-및-wsl-연결)
3. [WSL2 환경: 아두이노 시리얼 포트 확인 및 권한 설정](#3-wsl2-환경-아두이노-시리얼-포트-확인-및-권한-설정)
4. [파이썬 가상환경 및 Dependency 설치](#4-파이썬-가상환경-및-dependency-설치)
5. [센서 포트 설정 변경 (`sensor.py`)](#5-센서-포트-설정-변경-sensorpy)
6. [아두이노 하드웨어 동작 확인](#6-아두이노-하드웨어-동작-확인)
7. [Streamlit 애플리케이션 실행 및 테스트](#7-streamlit-애플리케이션-실행-및-테스트)

---

## 1. 사전 준비
* 아두이노가 PC의 USB 포트에 연결된 상태여야 합니다.
* Windows용 `usbipd-win` 설치가 완료되어 있어야 합니다.
* WSL2 (Ubuntu 등)가 설치 및 실행 가능한 상태여야 합니다.

---

## 2. Windows 환경: usbipd 설정 및 WSL 연결

### Step 1) Windows PowerShell (관리자 권한) 실행
PowerShell을 **관리자 권한**으로 실행합니다.

### Step 2) 연결된 USB 장치 목록 확인
```powershell
usbipd list
```
**출력 예시:**
```text
BUSID  VID:PID    DEVICE                                   STATE
1-3    2341:0043  Arduino Uno                              Not shared
2-4    ...
```
> 💡 **참고:** 아두이노 장치의 `BUSID`를 확인합니다. (예시에서는 `1-3`)

### Step 3) WSL 공유 설정 (최초 1회 실행)
아두이노 장치를 WSL과 공유 가능하도록 바인딩합니다.
```powershell
usbipd bind --busid 1-3
```

### Step 4) WSL에 장치 연결 (Attach)
WSL을 새로 켤 때나 아두이노 USB를 재연결할 때마다 실행합니다.
```powershell
usbipd attach --wsl --busid 1-3
```

**성공 메세지 예시:**
```text
usbipd: info: Using WSL distribution 'Ubuntu' to attach; the device will be available in all WSL 2 distributions.
usbipd: info: Detected networking mode 'nat'.
```

---

## 3. WSL2 환경: 아두이노 시리얼 포트 확인 및 권한 설정

### Step 1) 포트 인식 확인
WSL 터미널을 열고 아두이노 포트가 정상 인식되었는지 확인합니다.
```bash
ls /dev/ttyACM*
```
> `/dev/ttyACM0` 등의 디바이스 파일이 출력되면 정상적으로 연결된 것입니다.

### Step 2) 시리얼 포트 접근 권한 설정 (`dialout` 그룹 추가)
WSL 사용자 계정에 시리얼 포트 접근 권한을 부여합니다.
```bash
sudo usermod -a -G dialout $USER
```
> ⚠️ **주의:** 권한 반영을 위해 **WSL 터미널을 완전히 종료 후 재선택/재실행**해야 합니다.  
> 재시작 후 `groups` 명령어를 입력했을 때 결과에 `dialout`이 포함되어 있으면 정상 적용된 것입니다.

---

## 4. 파이썬 가상환경 및 Dependency 설치

프로젝트 디렉토리로 이동한 후 가상환경을 활성화하고 시리얼 통신 라이브러리(`pyserial`)를 설치합니다.

```bash
# 프로젝트 디렉토리 이동
cd ~/work/auto-parts-mes

# 가상환경 활성화 (프로젝트 가상환경 이름에 맞게 변경)
source venv/bin/activate

# pyserial 라이브러리 설치
pip install pyserial
```

---

## 5. 센서 포트 설정 변경 (`sensor.py`)

WSL에서 인식된 포트 이름에 맞게 소스 코드를 수정합니다.

```bash
nano src/sensor.py
```

파일 상단의 `DEFAULT_PORT` 값을 확인된 포트 경로로 지정합니다:
```python
DEFAULT_PORT = "/dev/ttyACM0"
```

---

## 6. 아두이노 하드웨어 동작 확인

* 아두이노 전원 및 데이터 케이블 연결 상태를 점검합니다.
* 센서 측정 코드가 정상 업로드되어 있는지 확인합니다.
* **LCD 화면 확인:** LCD 스크린에 `Dist: ...cm` 및 온습도 수치가 정상적으로 표시되고 있는지 확인합니다.

---

## 7. Streamlit 애플리케이션 실행 및 테스트

### Step 1) Streamlit 서버 실행
```bash
streamlit run app.py --server.address 0.0.0.0 --server.port 8502
```

### Step 2) 웹 브라우저 모니터링 확인
1. 브라우저를 열고 `http://localhost:8502` (또는 지정된 IP) 접속
2. **로그인** 진행 ➔ 메인 화면 접속
---
---


# WSL2 Streamlit 외부 접속 설정 

본 가이드는 WSL2 내부에서 실행되는 Streamlit 웹 애플리케이션을 Windows 포트 포워딩(`netsh portproxy`) 및 방화벽 설정을 통해 **동일 네트워크의 외부 기기(다른 PC, 스마트폰 등)**에서 접속할 수 있도록 설정하는 방법과 체크리스트를 다룹니다.

---

## 📋 목차
1. [0단계: Windows 이더넷 IP 확인](#0단계-windows-이더넷-ip-확인)
2. [1단계: WSL2 내부에서 Streamlit 실행](#1단계-wsl2-내부에서-streamlit-실행)
3. [2단계: WSL2 내부 IP 확인](#2단계-wsl2-내부-ip-확인)
4. [3단계: Windows PowerShell 포트 포워딩 설정 (`netsh`)](#3단계-windows-powershell-포트-포워딩-설정-netsh)
5. [4단계: Windows 방화벽 인바운드 규칙 추가](#4단계-windows-방화벽-인바운드-규칙-추가)
6. [5단계: 외부 기기 접속 테스트](#5단계-외부-기기-접속-테스트)
7. [💡 참고: USB 아두이노(`usbipd`) 연동 주의사항](#-참고-usb-아두이노usbipd-연동-주의사항)
8. [✅ 체크리스트](#-발표-당일-체크리스트)

---

## 0단계: Windows 이더넷 IP 확인

Windows PowerShell을 열고 호스트 PC의 IPv4 주소를 확인합니다.

```powershell
ipconfig
```
> **출력 예시:** `이더넷 어댑터`의 IPv4 주소 확인 (예: `10.10.21.23`)

---

## 1단계: WSL2 내부에서 Streamlit 실행

WSL2 터미널에서 가상환경을 활성화하고 외부 접속이 가능하도록 `--server.address 0.0.0.0` 옵션을 사용하여 실행합니다.

```bash
cd ~/work/auto-parts-mes
source mes/bin/activate
streamlit run app.py --server.address 0.0.0.0 --server.port 8502
```
> 💡 다른 앱(예: 라면 MES 등)이 `8501` 포트를 사용 중인 경우, 포트 충돌 방지를 위해 `8502` 등 다른 포트로 지정합니다.

---

## 2단계: WSL2 내부 IP 확인

WSL2의 새로운 터미널 창을 열고 internal IP를 확인합니다.

```bash
hostname -I
```
> **출력 예시:** `172.25.100.134`  
> ⚠️ **주의:** WSL2 IP는 시스템 재부팅이나 네트워크 재연결 시 변경될 수 있으므로 매번 확인이 필요합니다.

---

## 3단계: Windows PowerShell 포트 포워딩 설정 (`netsh`)

Windows에서 받은 요청을 WSL2 내부 IP로 전달하도록 포트 포워딩 규칙을 등록합니다.  
**Windows PowerShell을 관리자 권한으로 실행**한 후 명령어를 입력합니다.

### 1) 포트 포워딩 규칙 등록
```powershell
netsh interface portproxy add v4tov4 listenport=8502 listenaddress=0.0.0.0 connectport=8502 connectaddress=172.25.100.134
```
*(※ `connectaddress` 값에는 2단계에서 확인한 WSL IP를 입력합니다.)*

### 2) 기존 규칙이 있거나 IP가 변경되어 재등록해야 하는 경우
```powershell
# 기존 규칙 삭제
netsh interface portproxy delete v4tov4 listenport=8502 listenaddress=0.0.0.0

# 신규 규칙 재등록
netsh interface portproxy add v4tov4 listenport=8502 listenaddress=0.0.0.0 connectport=8502 connectaddress=172.25.100.134
```

### 3) 등록된 포트 포워딩 규칙 확인
```powershell
netsh interface portproxy show v4tov4
```

---

## 4단계: Windows 방화벽 인바운드 규칙 추가

외부 기기에서 8502 포트로 들어오는 통신을 허용하도록 Windows 방화벽 규칙을 추가합니다. (PowerShell 관리자 권한)

```powershell
New-NetFirewallRule -DisplayName "Streamlit AutoPartsMES" -Direction Inbound -LocalPort 8502 -Protocol TCP -Action Allow
```

---

## 5단계: 외부 기기 접속 테스트

동일한 Wi-Fi / LAN 네트워크에 연결된 다른 컴퓨터나 스마트폰의 웹 브라우저 주소창에 아래 주소를 입력하여 접속합니다.

```text
http://10.10.21.23:8502
```
*(※ 0단계에서 확인한 Windows 이더넷 IP와 지정한 포트 번호를 사용합니다.)*

---

## 💡 참고: USB 아두이노(`usbipd`) 연동 주의사항

현재 USB로 아두이노가 연결되어 있고 `usbipd attach`를 통해 WSL2에 바인딩된 상태라면, **포트 포워딩과 USB 연동은 독립적으로 작동하며 서로 충돌하지 않습니다.** 기존 연결 상태를 그대로 유지하시면 됩니다.

단, 발표 당일 PC를 재부팅할 경우 네트워크 설정 및 USB 연결이 초기화되므로 아래 순서대로 다시 설정해 주셔야 합니다.

---

## ✅  체크리스트

다음 사항들을 순서대로 점검하세요!

- [ ] **1. 아두이노 USB 연결 및 WSL 바인딩**
  - Windows PowerShell (관리자 권한) 실행 후 아두이노 연결 확인:
    ```powershell
    usbipd attach --wsl --busid 1-3
    ```
- [ ] **2. WSL2에서 Streamlit 서버 실행**
  - WSL 터미널에서 실행 확인:
    ```bash
    streamlit run app.py --server.address 0.0.0.0 --server.port 8502
    ```
- [ ] **3. WSL2 IP 재확인 및 포트 포워딩 재설정 (필요시)**
  - WSL2 터미널에서 IP 확인:
    ```bash
    hostname -I
    ```
  - WSL IP가 변경되었다면 3단계의 `netsh portproxy` 명령어로 재등록 진행
- [ ] **4. 포트 포워딩 규칙 활성화 점검**
  - Windows PowerShell에서 확인:
    ```powershell
    netsh interface portproxy show v4tov4
    ```
- [ ] **5. Windows 이더넷 IP 확인 및 시연 기기 접속 테스트**
  - Windows IP 확인:
    ```powershell
    ipconfig
    ```
  - 외부 스마트폰/PC 브라우저에서 `http://[Windows IP]:8502` 접속하여 모니터링 화면 동작 확인

4. **"현장 모니터링"** 카드 클릭
5. 모니터링할 **LOT 선택** (또는 *"LOT 선택 안 함"*)
6. **"모니터링 시작"** 버튼 클릭
7. 아두이노 LCD에 표시되는 거리/온도/습도 데이터가 Streamlit 대시보드 화면에 실시간으로 일치하여 갱신되는지 확인합니다.
